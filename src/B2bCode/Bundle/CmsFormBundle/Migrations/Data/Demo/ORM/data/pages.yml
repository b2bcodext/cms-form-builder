about:
    slug: feedback
    title: 'Feedback form'
    # there should be a {{ b2b_code_form("feedback-form") }} call inside a second diff, but in 6.0
    # pages are being validated when running demo data, and so it triggers error related to lack of request's session:
    # "There is currently no session available." exception.
    # Uncomment it to see the results
    content: >
        <div class="grid contact-us-form">
            <div class="grid-col-6"><img src="/bundles/b2bcodecmsform/images/feedbackform.jpg" alt="feedbackform.jpg"></div>
            <div class="grid-col-6">{# b2b_code_form("feedback-form") #}</div>
        </div>
